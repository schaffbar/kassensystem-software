export interface ActiveUser {
  customerId: string;
  firstName: string;
  lastName: string;
  entryTime: string;
}

export interface ActiveTool {
  toolId: string;
  toolName: string;
  userId: string;
  firstName: string;
  lastName: string;
}
