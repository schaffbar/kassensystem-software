<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <property resource="META-INF/build-info.properties" />
    <springProperty name="spring.application.name" source="spring.application.name" />
    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}" />
    <!-- define stack trace element exclusion patterns as a global property, list from https://github.com/logfellow/logstash-logback-encoder/blob/main/stack-hash.md -->
    <property name="STE_EXCLUSIONS" value="\$\$FastClassByCGLIB\$\$,\$\$EnhancerBySpringCGLIB\$\$,^sun\.reflect\..*\.invoke,^com\.sun\.,^sun\.net\.,^net\.sf\.cglib\.proxy\.MethodProxy\.invoke,^org\.springframework\.cglib\.,^org\.springframework\.transaction\.,^org\.springframework\.validation\.,^org\.springframework\.app\.,^org\.springframework\.aop\.,^java\.lang\.reflect\.Method\.invoke,^org\.springframework\.ws\..*\.invoke,^org\.springframework\.ws\.transport\.,^org\.springframework\.ws\.soap\.saaj\.SaajSoapMessage\.,^org\.springframework\.ws\.client\.core\.WebServiceTemplate\.,^org\.springframework\.web\.filter\.,^org\.apache\.tomcat\.,^org\.apache\.catalina\.,^org\.apache\.coyote\.,^java\.util\.concurrent\.ThreadPoolExecutor\.runWorker,^java\.lang\.Thread\.run$" />
    <appender name="default" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>
    <appender name="jsonAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timeZone>UTC</timeZone>
            <fieldNames>
                <timestamp>time</timestamp>
                <version>[ignore]</version>
            </fieldNames>
            <customFields>
				{"application.name":"${spring.application.name}", "application.version":"${build.version}"}
            </customFields>
            <!-- computes and adds a 'stack_hash' field on errors -->
            <provider class="net.logstash.logback.composite.loggingevent.StackHashJsonProvider">
                <!-- use global property for exclusion patterns -->
                <exclusions>${STE_EXCLUSIONS}</exclusions>
            </provider>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <maxDepthPerThrowable>30</maxDepthPerThrowable>
                <maxLength>4096</maxLength>
                <shortenedClassNameLength>25</shortenedClassNameLength>
                <rootCauseFirst>true</rootCauseFirst>
                <!-- enriches the stack trace with unique hash -->
                <!-- compute and inline hash in stack trace -->
                <inlineHash>true</inlineHash>
                <!-- use global property for exclusion patterns -->
                <exclusions>${STE_EXCLUSIONS}</exclusions>
            </throwableConverter>
        </encoder>
    </appender>
    <springProfile name="dev,stg,prd-eu">
        <root level="INFO">
            <appender-ref ref="jsonAppender" />
        </root>
    </springProfile>
    <springProfile name="local,test">
        <root level="INFO">
            <appender-ref ref="default" />
        </root>
    </springProfile>
    <logger name="jsonLogger" additivity="false" level="INFO">
        <appender-ref ref="jsonAppender" />
    </logger>
    <logger name="jsonLogger" additivity="false" level="INFO">
        <appender-ref ref="default" />
    </logger>
</configuration>
