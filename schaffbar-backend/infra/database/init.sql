-- Database initialization

-- Create a new database user
--CREATE USER ${POS_DB_USER} WITH ENCRYPTED PASSWORD ${POS_DB_PASSWORD};

-- Create schema
CREATE SCHEMA IF NOT EXISTS schaffbar;

-- Add more tables as needed
