# Deployment

## Local

git clone https://github.com/schaffbar/kassensystem.git

cd kassensystem

git checkout piotr

cd schaffbar-backend

create .env Datei mit folgendem Inhalt

docker compose up --build -d
